server:
  port: 8099


spring:
  application:
    name: stream
  cloud:
    stream:
      bindings:
        output:
          destination: info-topic  # 메시지를 보낼 Kafka 토픽 이름
          content-type: application/json
        input:
          destination: info-topic  # 메시지를 받을 Kafka 토픽 이름
          content-type: application/json
      kafka:
        binder:
          brokers: 192.168.100.221:30635  # Kafka 브로커 주소
eureka:
  instance:
    preferIpAddress: true
    lease:
      renewal:
        interval:
          in:
            seconds: 10
      expiration:
        duration:
          in:
            seconds: 30
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://team6-eureka-server:8761/eureka
